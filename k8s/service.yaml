apiVersion: v1
kind: Service
metadata:
  name: e2e-tutorial-service
  namespace: dev
  labels:
    app: e2e-tutorial
spec:
  type: NodePort
  selector:
    app: e2e-tutorial
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080